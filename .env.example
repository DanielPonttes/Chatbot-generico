# ====================================
# CONFIGURAÇÃO DO CHATBOT ECONÔMICO
# ====================================
# Copie este arquivo para .env e ajuste os valores conforme necessário
# cp .env.example .env

# ------------------------------------
# PROVIDER LLM (ollama ou huggingface)
# ------------------------------------
# Provider principal: "ollama" (recomendado, gratuito e local)
# Fallback: "huggingface" (requer token, usa API externa)
LLM_PROVIDER=ollama
# LLM_PROVIDER=huggingface

# ------------------------------------
# CONFIGURAÇÃO OLLAMA (recomendado)
# ------------------------------------
# URL base do Ollama (padrão: localhost)
OLLAMA_BASE_URL=http://localhost:11434

# Modelo Ollama a usar (opções leves recomendadas):
# - qwen2.5:0.5b  (menor, ~400MB, bom para PT-BR) <- PADRÃO
# - qwen2.5:1.5b  (melhor qualidade, ~1GB)
# - llama3.2:1b   (bom geral, ~700MB)
# - phi3:mini     (Microsoft, ~2GB)
OLLAMA_MODEL=deepseek-r1:latest

# ------------------------------------
# CONFIGURAÇÃO HUGGINGFACE (fallback)
# ------------------------------------
# Token da HuggingFace (obrigatório se usar provider huggingface)
# Obtenha em: https://huggingface.co/settings/tokens
# Deixe vazio se usar apenas Ollama
HF_TOKEN=

# Modelo HuggingFace para inferência
# Use modelos pequenos que suportam a API de inferência gratuita
HF_MODEL=microsoft/DialoGPT-small

# ------------------------------------
# CONFIGURAÇÃO DO BOT
# ------------------------------------
# System prompt que define a persona do bot
# Personalize conforme o contexto do seu projeto
BOT_SYSTEM_PROMPT=Você é um assistente virtual amigável e prestativo. Responda sempre em português brasileiro de forma clara e objetiva.

# ------------------------------------
# CONFIGURAÇÃO DE MEMÓRIA
# ------------------------------------
# Número máximo de mensagens mantidas por sessão (histórico)
MEMORY_MAX_MESSAGES=10

# Usar SQLite para persistir conversas? (true/false)
# Se false, conversas são perdidas ao reiniciar o servidor
USE_SQLITE=false

# Caminho do arquivo SQLite (usado apenas se USE_SQLITE=true)
SQLITE_PATH=./data/conversations.db
