version: '3.8'

services:
  chatbot:
    build: .
    container_name: chatbot-economico
    restart: unless-stopped
    network_mode: "host"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
